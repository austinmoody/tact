services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "2100:2100"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
      - TACT_DB_PATH=/app/data/tact.db
